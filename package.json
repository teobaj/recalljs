{
  "name": "recalljs",
  "private": false,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc -d && vite build",
    "preview": "vite preview",
    "test": "jest --watchAll --verbose"
  },
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "typescript": "^4.5.4",
    "vite": "^2.8.0",
    "jest": "^27.5.1"
  },
  "dependencies": {
    "rxjs": "^7.5.5"
  }
}
